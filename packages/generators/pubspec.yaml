name: generators
description: Getters and setters to save variables to and from Map
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  localize_it_annotation: ^1.0.0
  analyzer: ^5.1.0
  build:
  source_gen:
  http: ^0.13.5

dev_dependencies:
  build_test:
  build_runner:
  test: ^1.0.0
